# BB-0004: Screen Separation and Routing

## Background
Currently when user sees all projects or any project the url stays the same and there is no browser history generated between two. We need some kind of routing and isolation between screens. We may introduce more screens in the future.

## To-do
- Separate projects screen from project screen, giving the screen good names.
- Browser url bar should change when a user is on projects screen or in a particular project.
- Browser url bar should change between user is editing project a and project b.