# BB-0002: Add Import and Export

## Background
Currently this app runs on a users browser and there is no way of sharing projects with one another.

## Todo
- In projects screen add a way to export a project as a json file (use .bb extension).
- In projects screen add a way to import a json file as a new project.
- When importing project check the file:
    - do not allow if it's not the expected extension (.bb) using filters in the file selection dialog
    - do not allow if it's too big
    - do not allow if the contents are not parsable as json
    - do not allow id json is not in the structure we expect